// ZoneCard.jsx
// Displays a single zone's current status.
// Props: zone (object with zone_id, water_level, status)
// Renders a coloured status indicator (green / yellow / red) and latest reading.
